DirectShowPlayer README
====================

DirectShowPlayer is a simple application for playing DirectShow devices

## Build

```
git clone git@github.com:MonocleSecurity/DirectShowPlayer.git --recursive
cd DirectShowPlayer
VCPKG_FORCE_SYSTEM_BINARIES=1 cmake -G"Unix Makefiles" .
make
```

## Run

./DirectShowPlayer
